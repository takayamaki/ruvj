{
  "name": "RuVJ",
  "dockerComposeFile": "docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspaces/workspace",
"initializeCommand": "mkdir -p ~/.claude && if [ ! -f ~/.claude.json ]; then printf '{}\n' > ~/.claude.json; fi",
  "postCreateCommand": "bash .devcontainer/postCreateCommand.sh",
  "forwardPorts": [],
  "customizations": {
    "vscode": {
      "extensions": []
    }
  }
}
